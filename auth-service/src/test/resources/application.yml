spring:
  cloud:
    config:
      enabled: false

  datasource:
    url: jdbc:postgresql://localhost:5432/bank-users
    username: postgres
    password: password

eureka:
  client:
    enabled: false