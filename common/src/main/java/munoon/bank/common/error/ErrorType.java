package munoon.bank.common.error;

public enum ErrorType {
    REQUEST_EXCEPTION,
    VALIDATION_ERROR,
    DATA_ERROR,
    NOT_UNIQUE_ERROR,
    APPLICATION_EXCEPTION,
    ACCESS_DENIED,
    NOT_FOUND
}
